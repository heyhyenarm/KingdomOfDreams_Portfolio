using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Fish : MonoBehaviour
{
    private void OnTriggerEnter(Collider other)
    {
        //if(other.name != "fishingPoint" && other.name != "Water")
        //{
        //    Destroy(this.gameObject);
        //}
    }
}
